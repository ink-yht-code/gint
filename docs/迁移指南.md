# 迁移指南

这份文档用于说明从旧版 `gint` 示例迁移到当前结构时的主要方向。

## 文档结构

当前文档按主题划分为：

- 核心功能
- 认证授权
- 中间件
- 微服务支持
- 数据层
- 服务治理
- 开发工具

总入口请从 [docs/README.md](c:/Users/yanha/Desktop/tuojintech/code/go/ink-yht-code/gint/docs/README.md) 开始。

## 主要迁移方向

- 优先使用当前包装器：`W / B / S / BS`
- Session 体系优先参考 `session.Provider`
- 认证接入以当前认证中间件文档为准
- 错误处理统一到 `BizError` 与 `code/msg/data`

## 升级建议

1. 先把旧文档链接替换成新的分组路径。
2. 按当前中间件和 Session 模型重做认证接入。
3. 把业务错误统一整理到 `BizError` 体系。
4. 清理那些已经不符合现有代码的旧示例假设。
